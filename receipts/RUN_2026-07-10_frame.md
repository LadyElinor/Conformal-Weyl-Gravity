# RUN RECEIPT: frame_probe.py — 2026-07-10

## Purpose
Encodes the Hobson-Lasenby vs Mannheim dispute as two machine-checked facts,
per the design decision that the probe RENDERS the dispute and does not
adjudicate which conformal frame is physical.

## Environment
- Python 3.12.3, sympy 1.14.0

## Verbatim output
```
[FACT 1] Conformal map Omega = 1/(1 - a r):
    linear coefficient of Bhat(rho): (3*a*beta - 1)*(3*a*beta*gamma - 2*a - gamma)
    values of a eliminating it: [1/(3*beta), gamma/(3*beta*gamma - 2)]
    a = 1/(3*beta):
        rho^-1 coefficient: beta*(3*beta*gamma - 2)
        rho^0 coefficient: -1
        rho^2 coefficient: -(27*beta**2*kappa - 3*beta*gamma - 1)/(27*beta**2)
        SdS-type (no linear term): True
    a = gamma/(3*beta*gamma - 2):
        rho^-1 coefficient: beta*(3*beta*gamma - 2)
        rho^0 coefficient: 1
        rho^2 coefficient: -(9*beta**2*gamma**2*kappa - beta*gamma**3 - 12*beta*gamma*kappa + gamma**2 + 4*kappa)/(3*beta*gamma - 2)**2
        SdS-type (no linear term): True
    physical branch (SdS-type, constant = +1): a = [gamma/(3*beta*gamma - 2)]
    O(1) difference (must vanish): 0
[FACT 2] Rotation diagnostic v^2 = r B'/(2B):
    conformally invariant: False
    exact difference v2_MK(r) - v2_SdS(rho(r)) = (9*beta**2*gamma**2*r - 6*beta*gamma**2*r**2 - 6*beta*gamma*r + gamma**2*r**3 + 2*gamma*r**2)/(-18*beta**3*gamma**2 + 18*beta**2*gamma**2*r + 24*beta**2*gamma - 6*beta*gamma**2*r**2 + 6*beta*gamma*kappa*r**3 - 18*beta*gamma*r - 8*beta + 4*gamma*r**2 - 4*kappa*r**3 + 4*r)
    first order in (beta, gamma, kappa): gamma*r/2

READING: the frames agree on the Newtonian 1/r physics and
disagree by terms proportional to gamma -- the linear-potential
rotation support is precisely the frame-dependent piece.

RECEIPT SUMMARY: {'conformal_map_to_SdS': True, 'diagnostic_frame_dependent': True}
```

## Findings ledger
1. Conformal map derived, not assumed: ansatz-preserving factors satisfy
   Omega + r Omega' = Omega^2, so Omega = 1/(1 - a r). Two branches kill the
   linear term; the physically normalized one (constant coefficient +1) is
   a = gamma/(3 beta gamma - 2), which tends to -gamma/2 as beta -> 0.
2. The map preserves the Newtonian charge exactly: the 1/rho coefficient of
   the SdS-frame metric equals MK's own u = -beta(2 - 3 beta gamma).
3. The static-observer rotation diagnostic v^2 = r B'/(2B) is NOT
   conformally invariant. O(1) difference vanishes (flat-limit sanity);
   the exact difference is proportional to gamma overall; its leading term
   in the small parameters is EXACTLY gamma*r/2 -- the entire linear
   rotation-support term and nothing else.

## Scope statement
This establishes that the fitted rotation quantity is frame-dependent under
ansatz-preserving conformal maps, which is the computable premise of the
Hobson-Lasenby critique (PRD 104, 064014, 2021). It does NOT determine which
frame's diagnostic corresponds to measured kinematics; that depends on the
mass-generation mechanism, which is the live Mannheim (GRG 54, 99, 2022)
counter-position. Adjudication would require modeling particle mass profiles,
which this probe deliberately does not do.

Test suite after addition: 12 passed.
